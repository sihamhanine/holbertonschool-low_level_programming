#include "main.h"

/**
 * *_memcpy - Function that copies memory area.
 *
 * @dest: adresse de destination
 * @src: adresse source
 * @n: le nombre a dupliquer
 *
 * Return: dest 
 */
cah *_memcpy(char *dest, char *src, unsigned int n);
