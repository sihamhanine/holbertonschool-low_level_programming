#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
  char c;
  for(c = 'a'; c <= 'z' ; c++)
    {
      putchar(c);
    }
}
