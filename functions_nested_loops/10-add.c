#include "main.h"

/**
 * add - Function that adds two integers
 * and returns the result.
 * @x: entier a verifier.
 * @y: entier a verifier
 *
 * Return: la somme de deux nombre.
 */
int add(int x, int y)
{
int sum;
sum = x + y;
return (sum);
}
